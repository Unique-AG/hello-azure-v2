spec:
  name: zitadel
  autoSync: false
  sources:
    - repoURL: 'https://github.com/Unique-AG/hello-azure-v2'
      targetRevision: chore/refactor-after-migration
      ref: values
    - chart: zitadel
      repoURL: https://charts.zitadel.com
      targetRevision: 8.13.1
      helm:
        releaseName: zitadel
        valueFiles: 
          - $values/03_applications/test/values/zitadel/zitadel-extra-manifests.yaml
          - $values/03_applications/test/values/zitadel/zitadel.yaml
        valuesObject: {} 
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "3"