spec:
  name: backend-service-webhook-worker
  autoSync: false
  sources:
    - repoURL: 'https://github.com/Unique-AG/hello-azure'
      targetRevision: preview
      ref: values
    - repoURL: ghcr.io/unique-ag/helm-charts
      chart: backend-service
      targetRevision: 6.0.1
      helm:
        releaseName: backend-service-webhook-worker
        valueFiles:
          - $values/applications/defaults/backend-services/webhook-worker.yaml
          - $values/applications/test/values/backend-services/_all.yaml
          - $values/applications/test/values/backend-services/webhook-worker.yaml
        valuesObject: {}
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "5"